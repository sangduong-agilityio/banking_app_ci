import 'package:flutter/material.dart';

class LetStartedPage extends StatelessWidget {
  const LetStartedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
