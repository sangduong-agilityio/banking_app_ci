/// A class that contains the keys used for storing data in shared preferences.
class PrefKeys {
  /// The key for the session token.
  static const sessionToken = 'session_token';

  /// The key for the biometric enabled flag.
  static const biometricEnabled = 'biometric_enabled';

  /// The key for the biometric refresh token.
  static const biometricRefreshToken = 'biometric_refresh_token';
}
